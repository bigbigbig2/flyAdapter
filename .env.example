# GR3 adapter defaults. Keep namespace aligned with HumanoidNav startup.
ROBOT_NAMESPACE=GR301AA0025
ADAPTER_HOST=0.0.0.0
ADAPTER_PORT=8080

# HumanoidNav map/runtime locations on the robot.
MAP_ROOT=/opt/fftai/nav
DEFAULT_MAP_PATH=/opt/fftai/nav/map
ADAPTER_DATA_DIR=./data

# Aurora is best-effort by default so HTTP debugging can run before SDK setup.
# Set to 1 on the real robot if navigation must be blocked when Aurora is unavailable.
REQUIRE_AURORA=0
AURORA_ROBOT_NAME=gr3v233
AURORA_DOMAIN_ID=123
# Your docs note that Aurora SDK calls usually need to execute inside the
# AuroraCore container. Keep docker as the default backend on the real robot.
AURORA_BACKEND=docker
AURORA_CONTAINER_NAME=fourier_aurora_server
AURORA_CONTAINER_WORKDIR=/workspace
AURORA_CONTAINER_PYTHON=python3
AURORA_DOCKER_TIMEOUT_SEC=20
AURORA_DOCKER_LOCK_TIMEOUT_SEC=0.2
AURORA_STATE_CACHE_TTL_SEC=10
AURORA_STATE_STALE_TTL_SEC=120

# Optional for AURORA_BACKEND=python, or when the in-container module/class
# name differs from the defaults.
AURORA_SDK_PATH=
AURORA_CLIENT_MODULE=fourier_aurora_client
AURORA_CLIENT_CLASS=AuroraClient

# Set AURORA_MOCK=1 only for local adapter UI/API testing.
AURORA_MOCK=0
